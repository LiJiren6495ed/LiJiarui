{
    "tasks": [
        {
            "type": "shell",
            "label": "tast gcc",  // 编译任务的名称，跟上面preLaunchTask的值对应
            "command": "D:/Downloads/x86_64-8.1.0-release-posix-seh-rt_v6-rev0/mingw64/bin/g++.exe",  // 编译器的路径，须跟你自己的电脑相符
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}